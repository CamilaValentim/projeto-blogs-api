// const { Categorie } = require('../models');

// const categoriaExist = async (req, res) => {
//      const catogoria = await Categorie.findAll();

//      categoria.map(categoria.id)
//             res.status(200).json(categoria);
//     };

// module.exports = categoriaExist;